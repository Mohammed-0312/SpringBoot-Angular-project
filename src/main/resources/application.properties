# Application Name
spring.application.name=MultipleDB

# Server Port
server.port=8080

# Primary DataSource Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/multiple_db
spring.datasource.username=root
spring.datasource.password=1hk20cs098
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Second DataSource Configuration (PostgreSQL)
second.datasource.url=jdbc:postgresql://localhost:5432/multiple_db
second.datasource.username=postgres
second.datasource.password=root
second.datasource.driver-class-name=org.postgresql.Driver

# Hibernate properties for MySQL (These can be moved to the config class if needed)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Enable debug mode for detailed logs (optional)
debug=true
